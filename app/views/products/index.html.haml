%h1 Listing products

%table
  %tr
    %th Name
    %th Weight
    %th Quantity
    %th Cost
    %th Freight modifier
    %th Price modifier
    %th
    %th
    %th

  - @products.each do |product|
    %tr
      %td= product.name
      %td= product.weight
      %td= product.quantity
      %td= product.cost
      %td= product.freight_modifier
      %td= product.price_modifier
      %td= link_to 'Show', product
      %td= link_to 'Edit', edit_product_path(product)
      %td= link_to 'Destroy', product, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Product', new_product_path
